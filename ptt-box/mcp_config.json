{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:/app/ptt/ptt-box/sandbox"]
    },
    "time": {
      "command": "uvx",
      "args": ["mcp-server-time", "--local-timezone", "Asia/Tokyo"]
    },
    "memory": {
      "command": "uvx",
      "args": ["basic-memory", "mcp", "--project", "main"]
    },
    "tavily": {
      "command": "npx",
      "args": ["-y", "tavily-mcp@latest"],
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}"
      }
    },
    "dbhub-mysql": {
      "command": "npx",
      "args": [
        "-y", "@bytebase/dbhub@latest",
        "--transport", "stdio",
        "--config", "C:/app/ptt/ptt-box/dbhub.toml"
      ]
    }
  }
}
